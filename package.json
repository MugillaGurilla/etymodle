{
  "devDependencies": {
    "@types/cypress": "^0.1.6",
    "cypress": "^14.5.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "type": "module",
  "scripts": {
    "compile": "npx tsc",
    "clean": "rm -rf ./dist",
    "word:new": "npx ts-node dist/src/scripts/new-word.js",
    "word:old": "git checkout --  answer/today.json",
    "cypress:ui": "cypress open",
    "cypress:cli": "cypress run"
  }
}
